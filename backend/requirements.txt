# ── Core serving ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=21.0.0
python-multipart>=0.0.9

# ── ML stack ──────────────────────────────────────────────────────────────────
# PyTorch: install the right build for your platform from https://pytorch.org
# CPU-only (default, works everywhere):
torch>=2.1.0
torchvision>=0.16.0

# GPU (CUDA 12.1): uncomment and remove torch/torchvision above
# torch==2.3.0+cu121 --extra-index-url https://download.pytorch.org/whl/cu121
# torchvision==0.18.0+cu121 --extra-index-url https://download.pytorch.org/whl/cu121

# EfficientNet model zoo (Apache 2.0)
timm>=1.0.3

# ── Utilities ─────────────────────────────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
requests>=2.31.0

# ── Video support (choose one or both) ───────────────────────────────────────
# Option A: OpenCV (recommended — faster, more formats)
opencv-python>=4.9.0.80

# Option B: imageio with ffmpeg (lighter install)
# imageio[ffmpeg]>=2.34.0
